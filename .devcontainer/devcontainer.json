{
  "name": "C Development",
  "image": "mcr.microsoft.com/devcontainers/c:1-bullseye",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cpptools-themes"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y qemu-user gcc-arm-linux-gnueabi && gcc --version && arm-linux-gnueabi-gcc --version && qemu-arm --version"
}
